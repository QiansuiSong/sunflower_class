# 向日葵中班 DESIGN

## 项目固定架构

- index.html
- style.css
- data.js
- game.js
- DESIGN.md
- README.md

不得为了普通内容更新继续拆分文件。

## 游戏定位

玩家是刚转入“向日葵中班”的小朋友。每局代表一个短学期。玩家通过每日行动和剧情选择，与同学建立关系、形成个人特点，并共同完成期末开放日。

核心体验不是数值最优化，而是：

1. 从陌生人逐步进入班级关系网。
2. 选择必然意味着错过，无法一次看完全部关系。
3. 早期决定在后续事件中产生具体影响。
4. 期末用奖项、活动回忆和最好的朋友总结玩家走过的路线。

## 固定内容原则

- 全部角色均为向日葵中班小朋友。
- 人物特点必须通过行为、选择和具体反应体现。
- 故事原创，不直接照搬现实事件。
- 所有事件必须有完整起因、经过、结果。
- 不使用“没人知道为什么”“据说”等悬空叙事。
- 冲突不依赖恶人推动，主要来自目标、方法、节奏和表达差异。
- 选项不设置显而易见的唯一正确答案；不同方案应损失或获得不同内容。

## 核心循环

每日：恢复少量精力 → 选择一个日常行动 → 获得能力与关系 → 触发剧情事件 → 记录回忆 → 进入下一天。

学期共 18 天，分为四段：

- 第 1–5 天：报到与相识。
- 第 6–11 天：活动方案与合作摩擦。
- 第 12–16 天：暴雨危机与关系检验。
- 第 17–18 天：最终彩排、开放日与结算。

## 数据边界

`data.js`：人物、章节、每日行动、事件、后续事件、奖项。

`game.js`：状态机、存档、条件判定、数值结算、界面渲染。

`style.css`：视觉与响应式布局。

新增普通事件时不得修改 `game.js`。只有出现当前数据格式无法表达的新机制时，才允许最小范围修改。

## 事件结构

每个事件至少包含：

- `id`：唯一标识。
- `day`：允许出现的日期区间。
- `title`、`text`：完整情境。
- `characters`：出场人物。
- `choices`：至少两个选择。
- 每个选择包含结果文本与明确效果。

可选：

- `require`：能力门槛。
- `flags`：记录关键选择。
- `requireFlag`：后续事件的前置标记。

## 后续扩展优先级

1. 继续在 `data.js` 增加事件，目标为主事件 25–35 个、条件后续事件 12–18 个。
2. 为每名角色建立至少一条两段式个人事件线。
3. 增加第二学期时，仍保持同一引擎，通过数据切换章节，不创建新页面或新脚本。

## 剧情版 v2 扩充规则

### 当前内容规模

- 普通/主线事件：26
- 条件后续事件：12
- 事件总数：38
- 主要结局：12（互斥，每局一个）
- 奖项：20（可叠加）

### 重玩机制

游戏使用独立的跨周目收藏存档，记录已经见过的事件、获得的主要结局和奖项。重新开始只清除当前学期，不清除收藏。

同一天存在多个可触发事件时，系统优先从尚未收集的事件中随机选择。条件后续事件优先于普通事件，因此前期选择会改变后半段故事。

### 内容设计原则

1. 新事件应尽量包含后续影响，不只提供一次性的数值变化。
2. 主要结局必须描述一条完整路线，不能只是最高属性换一句话。
3. 稀有结局需要多个跨阶段条件，避免单次选择直接解锁。
4. 奖项用于肯定具体行为，可以与主要结局同时获得多个。
5. 不增加新的代码文件；普通内容扩充优先仅修改 `data.js`。
